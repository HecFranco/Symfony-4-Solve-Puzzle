{% extends 'base.html.twig' %}
{% block body %}
    {{dump()}}
    {{ form_start(form) }}
        {{form_widget(form.file)}}
        {{form_widget(form.submit)}}
    {{ form_end(form) }}
{% endblock %}